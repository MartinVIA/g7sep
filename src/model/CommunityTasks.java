package model;

public class CommunityTasks extends Task {
    // some attributes?

    public CommunityTasks(String name, String type, int amountOfPoints) {
        super(name, type, amountOfPoints);
    }
}